this.queryService
.select(
qProject,
qProjectEpisode
)
.from(qProject)
.from(qProjectEpisode)
.where(qProjectEpisode.projectId.eq(qProject.projectId))
.orderBy(qProject.regDatetime.asc())
.orderBy(qProjectEpisode.regDatetime.desc())